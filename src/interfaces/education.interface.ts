export interface Education {
    id: number,
    institutionName: string,
    careerName: string
    institutionLogo: string,
    grade: string,
    years: number,
    hours: number,
    planEstudio: object,
    planEstudioPDF: string,
    startedAt: string
}